document.addEventListener("DOMContentLoaded", function(event) {
    // variables
    // inputs
    // Units
    let unit_metric_elem = document.getElementByClassName('units--metric-input')[0];
    let unit_imperial_elem = document.getElementByClassName('units-imperial-inpu')[0];
    // Block up weight + number of boards -> buw
    let buw_ct300_volume_elem = document.getElementById('input--buw_nob-1');
    let buw_ct8504_volume_elem = document.getElementById('input--buw_nob-2');
    let buw_ct8502_colume_elem = document.getElementById('input--buw_nob-3');

});
